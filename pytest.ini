[pytest]
markers =
    admission_notice_ocr_af_unix: requires a real local AF_UNIX bind/connect path
    admission_notice_ocr_integration: runs the real main-client to sidecar AF_UNIX path
    admission_notice_ocr_qualification: runs the real CPU Paddle qualification matrix
